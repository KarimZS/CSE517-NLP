1) unzip the project 

2) open command line

3) Go to the output directory of project (csharpHW\hw3\bin\Debug)

4) There should be a precompiled exe file.

5)./hw3.exe test. This will run the tagger on test dataset. You can try train and dev as well. These are for the small datasets. To run on full you will have to edit the code and recompile it. 

6) Take a look at the code to see implementation details or to change the number of iterations to train on. You can also turn features on and off by commenting on them out

7) use visual studio to rebuild and rerun it with any changes you want to try.